{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx", ".expo/types/**/*.ts", "expo-env.d.ts"],
  "//exclude": "supabase/functions is Deno, not React Native — it uses .ts import extensions and the Deno global, which this config cannot resolve. It is typechecked by the Supabase CLI at deploy time instead.",
  "exclude": ["backend", "supabase/functions"]
}
